The next step is to add some code which runs a loop of tasks. The user will put some files in a directory, and this code will watch that directory and pass it as a prompt to a relevant agent defined elsewhere in the codebase. The outputs of these prompts will conditionally be returned to the user to answer questions or will be passed straight to the another agent depending on a map of agent connections yet to be defined.