# Intro

There are two main files. 

1. chat
2. jobber

# Chat `chat.py`

Can make payloads, send them off to LM Studio and get them back.

Run `python -m swarmic_agents.chat.chat` to see if the server is working.

# Jobber `jobber.py`

